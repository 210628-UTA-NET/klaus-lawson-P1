@{
    if (User.Identity.IsAuthenticated)
    {

    <h2>My Profile: @User.Identity.Name</h2>
        <ul>
            @foreach (var c in User.Claims)
            {
                <li>@c.Type: @c.Value </li>
            }
        </ul>
    }
}